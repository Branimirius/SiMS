<Window x:Class="BolnicaSims.ListaTermina"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BolnicaSims"
        mc:Ignorable="d"
        Title="Lista Pregleda" Height="450" Width="1000">
    <Grid Background="#FF278FBF" >
        <DataGrid x:Name="dataGridTermini" ItemsSource="{Binding Source=Termini}" IsReadOnly="True"
                  SelectionMode="Single" AutoGenerateColumns="False" HorizontalAlignment="Center"
                  Height="352" Margin="0,72,0,0" VerticalAlignment="Top" Width="980">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdTermina}"  />
                <DataGridTextColumn Header="Datum" Binding="{Binding VremeTermina}" />
                <DataGridTextColumn Header="Doktor" Binding="{Binding ImePrezimeDoktora }" Width="*" />

            </DataGrid.Columns>

        </DataGrid>
        <Button Content="Zakazi pregled" HorizontalAlignment="Left" Height="31" Margin="10,21,0,0" VerticalAlignment="Top" Width="100" RenderTransformOrigin="0.512,0.679" Click="ButtonZakazi_Click" Background="White"/>
        <Button Content="Izmeni pregled" HorizontalAlignment="Left" Height="31" Margin="115,21,0,0" VerticalAlignment="Top" Width="100" Click="ButtonIzmeni_Click" Background="#FFFFFDFD"/>
        <Button Content="Otkazi pregled" HorizontalAlignment="Left" Height="31" Margin="220,21,0,0" VerticalAlignment="Top" Width="100" Click="ButtonOtkazi_Click" Background="#FFFFFDFD"/>
        <Label Content="Tabela predstojecih pregleda" HorizontalAlignment="Left" Margin="641,11,0,0" VerticalAlignment="Top" Width="349" Height="51" FontSize="24" FontWeight="Bold"/>


    </Grid>
</Window>
